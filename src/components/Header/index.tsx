import { Flex } from '@chakra-ui/react'
import { Title } from './Title'

export function Header() {
  return (
    <Flex>
      {/* Logo componente dentro da pasta header*/}
      {/* Profile */}
      <Title />
    </Flex>
  )
}